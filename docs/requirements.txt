# Installed by Read the Docs in addition to pip install ".[docs]"
# Keep in sync with [project.optional-dependencies] docs in pyproject.toml
sphinx>=7.0,<9
sphinx-rtd-theme>=2.0,<4
myst-parser>=3.0,<5
httpx>=0.27
fastapi>=0.100
